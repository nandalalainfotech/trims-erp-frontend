import { BaseEntity } from "./BaseEntity";

export class Role001mb extends BaseEntity {
  // roleid?: number;
  // rolename?: string;
  // status?: number;
  id?: number;
  unitslno?: number;
  rolename?: string;
}